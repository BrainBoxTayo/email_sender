import 'package:flutter/material.dart';

class EmailPageSend extends StatelessWidget {
  const EmailPageSend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
